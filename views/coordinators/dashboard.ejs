<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coordinators</title>
    <style>
        body {
            background-color: #0c9;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            flex-direction: column; /* Align content vertically */
        }

        table {
            background-color: #215858;
            border-collapse: collapse;
            width: 80%;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #0c9;
            padding: 8px;
            color: white;
        }

        th {
            background-color: #0c9;
        }

        h1, h2 {
            border-bottom: 2px solid #0c9;
            padding-bottom: 10px;
            text-align: center; /* Center align the text */
        }

        form {
            margin-top: 20px;
            text-align: center; /* Center align the form */
        }

        button {
            background-color: #0c9;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
        }

        button:hover {
            background-color: #215858;
        }
    </style>
</head>
<body>
    <% if (events && events.length > 0) { %>
        <h1>Club Name: <%= c_name %></h1>
        <h2>Club ID: <%= c_id %></h2>
        <table border="1">
            <thead>
                <tr>
                    <th>Event Name</th>
                    <th>Starting Date</th>
                    <th>Ending Date</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < events.length; i++) { %>
                    <tr>
                        <td><%= events[i].e_name %></td>
                        <td><%= new Date(events[i].e_startDate).toLocaleDateString() %></td>
                        <td><%= new Date(events[i].e_endDate).toLocaleDateString() %></td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <form action="/coordinators/add_event" method="post">
            <input type="hidden" name="c_id" value="<%= events[0].c_id %>">
            <button type="submit">ADD EVENT</button>
        </form>
    <% } else { %>
        <p>NO EVENTS YET</p>

        <form action="/coordinators/add_event" method="post">
            <input type="hidden" name="c_id" value="<%= c_id %>">
            <button type="submit">ADD EVENT</button>
        </form>
    <% } %>
</body>
</html>
