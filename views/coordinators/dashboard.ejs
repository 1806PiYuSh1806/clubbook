<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coordinators</title>
    <style>
        body {
            background-color: rgba(12, 201, 153, 0.5); /* Light green with texture */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            flex-direction: column; /* Align content vertically */
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        table {
            background-color: #f0f0f0; /* Light grey */
            border-collapse: collapse;
            width: 80%;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ccc; /* Light grey border */
            padding: 8px;
            color: #333; /* Dark grey text color */
        }

        th {
            background-color: #0c9; /* Dark blue */
            color: white; /* White text */
        }

        h1 {
            /* border-bottom: 2px solid #0c9; Dark blue underline */
            padding-bottom: 10px;
            text-align: center; /* Center align the text */
            color: #1a1a1a; /* Dark grey text color */
            font-size: 28px; /* Larger font size */
            font-weight: bold; /* Bold font weight */
            margin-bottom: 20px; /* Increased bottom margin */
        }

        h2 {
            border-bottom: 2px solid #0c9; /* Dark blue underline */
            padding-bottom: 10px;
            text-align: center; /* Center align the text */
            color: #0c9; /* Dark blue text color */
        }

        form {
            margin-top: 20px;
            text-align: center; /* Center align the form */
        }

        button {
            background-color: #0c9;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
        }

        button:hover {
            background-color: #215858;
        }
    </style>
</head>
<body>
    <% if (events && events.length > 0) { %>
        <h1>Club Name: <%= c_name %></h1>
        <table border="1">
            <thead>
                <tr>
                    <th>Event Name</th>
                    <th>Starting Date</th>
                    <th>Ending Date</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < events.length; i++) { %>
                    <tr>
                        <td><%= events[i].e_name %></td>
                        <td><%= new Date(events[i].e_startDate).toLocaleDateString() %></td>
                        <td><%= new Date(events[i].e_endDate).toLocaleDateString() %></td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <form action="/coordinators/add_event" method="post">
            <input type="hidden" name="c_id" value="<%= events[0].c_id %>">
            <button type="submit">ADD EVENT</button>
        </form>
    <% } else { %>
        <p>NO EVENTS YET</p>

        <form action="/coordinators/add_event" method="post">
            <input type="hidden" name="c_id" value="<%= c_id %>">
            <button type="submit">ADD EVENT</button>
        </form>
    <% } %>
</body>
</html>
